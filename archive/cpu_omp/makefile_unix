CXX = g++

CXXFLAGS = -O2 -fopenmp -std=c++17

TARGET = bitonic_openmp

SRCS = muti_random.cpp

OBJS = $(SRCS:.cpp=.o)


all: $(TARGET)

$(TARGET): $(OBJS)
    $(CXX) $(CXXFLAGS) -o $@ $^

%.o: %.cpp
    $(CXX) $(CXXFLAGS) -c $< -o $@

clean:
    rm -f $(OBJS) $(TARGET)